using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class theuselesshero : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        string heroname = "useless";
        int heroage = 16;
        float heroheight = 1.17f;
        string herosuperpower = "friendship";


        print("my name is" + heroname + "and my age is" + heroage + "and my height is" + heroheight + "and my super power is" + herosuperpower);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
